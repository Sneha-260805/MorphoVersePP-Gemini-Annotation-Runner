[pytest]
testpaths =
    tests

# `.` lets `import morphoverse_gemini_pipeline.delivery.poem_annotator...`
# resolve the same way `python -m morphoverse_gemini_pipeline.delivery.poem_annotator.corpus_gemini_runner_v1_1`
# resolves it from the repo root.
pythonpath =
    .

python_files = test_*.py
addopts = -q
